#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/manifest.v2.schema.json

packaging_format = 2

id = "openwebui"
name = "Open WebUI"
description.en = "User-friendly AI chat interface supporting Ollama and OpenAI-compatible APIs"
description.fr = "Interface de chat IA conviviale supportant Ollama et les API compatibles OpenAI"

version = "0.8.10~ynh1"

maintainers = []

[upstream]
license = "MIT"
website = "https://openwebui.com"
admindoc = "https://docs.openwebui.com"
userdoc = "https://docs.openwebui.com"
code = "https://github.com/open-webui/open-webui"

[integration]
yunohost = ">= 12.1.17"
helpers_version = "2.1"
architectures = "all"
multi_instance = true
ldap = true
sso = true
disk = "1000M"
ram.build = "1500M"
ram.runtime = "256M"

[install]
    [install.domain]
    type = "domain"

    [install.path]
    type = "path"
    default = "/"

    [install.admin]
    type = "user"

    [install.auth_mode]
    type = "select"
    choices.sso = "YunoHost SSO — only YunoHost users can log in"
    choices.open = "Open registration — anyone can create an account in Open WebUI"
    default = "sso"
    help.en = "SSO uses YunoHost accounts and single sign-on. Open registration lets anyone who can reach the app create their own account."
    help.fr = "SSO utilise les comptes YunoHost et l'authentification unique. L'inscription ouverte permet à quiconque pouvant accéder à l'application de créer son propre compte."

    [install.init_main_permission]
    type = "group"
    default = "visitors"
    help.en = "If set to 'visitors', anyone can access the login page. If set to a specific group, only those YunoHost users can access the app via SSO."
    help.fr = "Si défini sur 'visitors', tout le monde peut accéder à la page de connexion. Si défini sur un groupe spécifique, seuls ces utilisateurs YunoHost peuvent accéder à l'application via SSO."

[resources]
    [resources.system_user]

    [resources.install_dir]

    [resources.data_dir]

    [resources.permissions]
    main.url = "/"

    [resources.ports]

    [resources.apt]
    packages = "python3, python3-venv, python3-pip, python3-dev, build-essential, libffi-dev, postgresql"

    [resources.database]
    type = "postgresql"
